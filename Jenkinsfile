pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                  echo 'Obtaining Maven version now.'
                  sh 'mvn --version'
                }
            }
        }
    }
}
